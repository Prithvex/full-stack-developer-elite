React is the js library used to create UI --> dev by Facebook(meta)
problem --> reload ke baad update hota tha (likes , comment , view count)
Js Essentials (prereq..)
    basic , functions , arrays , objects , ES6+features , Asynchronous , Javascript
 
React js is the 
library --> GSAP, Lenis, ReactJS --> use for one particular framework 
framework --> Next.js , Angular --> everything at one section , everything is customised 

#import Export 
- script ka type module kardo 
- default import and export -- direct default export karo
- named  import and export -- {}

# Real DOM vs Virtual DOM
--> Virtual DOM --> real dom ki copy banata hain aur jab chenges karna ho to sirf uss part me hoga pura render nahi hoga 

# jsx --> Javascript XML --> (HTML + Javascript)

# Making folder with Vite --> it is the bundler
npm create vite
--> app.jsx me function banao aur export default kar do!
 
 index.html - main.jsx - app.jsx

 #####
 to print/ get any variable then {} curly braces me likho 
 ## Use 'rafce' --> to get the jsx function boilerplate 

 # Props Drilling --> ek component to bar bar use karo alag data ke sath 
 --> creating the cards 

 --> to print integer use this {}

# Project for Props Drilling

# Lucid React library --> for the good items 

# props and Basic Card project 

# CSS ---> global , inline , external 
-> every small section have their own css 
--> create seprate css file and import kar do 
--> module css use kiya karo !!

# Tailwind CSS
--> Tailwind is the utility first library 

--> Tags ko change karte hain 

#function
--> strict mode ko hata do 

#Hooks --> is the special type of function use to perform special tasks
* usestate --> manage the states in realtime 
* useeffect --> side by side road pe function chala do side by side 
* useRef --> Use to select DOM element, ye referance lets hain
* useContext --> Data Globally bhej do aur jisko bhi access karna hoga vo direct globally karega 
* useReducer --> use to manage the complex data state
* useMemo & useCallback--> Use to manage the optimisation 

# Arrays jo hote hain vo specific jagah se reference lete hainn to when jiss jagah se vo ref. lete hain vahha change hua to array me bhi affect hoga 

#Two way Binding Concept --> 

#notes-app -->  copy banao aur copy me data ko push kar do 
                setTask(copytask) --> copy ko original se replace kar do 
                to print --> task.map( function(){
                    return "hero" // hero print hoga fir notes ko submit karenge to 
                })
                -> div ko return karro
                -> div key={idx}
                --> <h1> { elem.title} </h1>
                --> <p> { elem.details} </p>

#Local Storage --> Store the choti si user ki information in the User Side for the fast and efficient data use case --> like account login, and all
--> localstorage.clear() - clear the local storage 
--> localStorage.setItem('user','prithvii') // hame key and value done deni padti hain 
--> const s= localstorage.getItem('key de do sirf")
--> console.log(s);
--> localStorage.removeItem('user') 
--> JSON.stringify and then JSON.parse

#API Calling --> Application Programming Interface 
--> Backend ko frontend se jodta hain API (Data Sharing Between them)
--> Backend ka data frontend pe effectively show karna hain 
------> Fetch , axios (Try Json placeholder for free api) 
--> Promise the type of datatype in js 
--> data raste main hain , js Asynchronous work karti hain 
--> to avoid this cconflict use 
    async and awaits 
    async function aap tab tak mat badna ja tak awaits na jo jaye 

-->Use the axios --> 
    npm i axios
    import axios form axios
    const res= axios.get("API HERE")
    console.local(res)

#UseEffect Hook --> 
use effect provide the foothpath for the side process 
main process ke side by side chalne do 
so ye process ko side wale foothpath pe store kar deta hain aur jab bhi main process rukti hain ye 
foothpath ki process ko sta

--> useEffect ke sath dependency ka array jarur likho (Blank array[])
--> [num] is the dependency
--> use effect ek hi baar chalta hain fir 


#Gallary Project --> 
import Tailwind
lorem picsum API --> for image data 
import axios --> api ke data ko khich ke le aao
axios.get --> get the data from the backend 
axios.post --> backend ko request bhejte hain 
//POSTMAN is the software use to check the api requests
axios.patch--> Update the data

//Images slow aat hain --> Lazy Loading 
//check card.jsx

#Routing in react 
    React_Router_DOM  --> its the react routing library 
    npm i react-router-dom

browser Router --> browser me history ko store karke rakhta hain 
    sheryians.com = k
    sheryians.com/courses = /courses
    sheryians.com/about = /about

HashRouter --> Bas pehle # aa jata hain
    sheryians.com = /#/KingsPage

MemoryRouter --> 

StaticRouter --> help in static sites

..In Main.jsx..
import BrowserRouter
<Browser Router> <App/> </Browser Router> 


..src..(pages - this is filename)..files(home, about, contact)

..In App.jsx..
import Routes
    <Routes>
    <Route path='/' element={<Home />} />
    </Routes>

react me link tag hota hain (ab reload nahi hoga )
<link to='/' > Home </link> --> ess se single page application ban gya no need multiple jump

basic setup
nested routes
dynamic routes
usenavigate
404 page


#Adv. Routing 
import Tailwind 
src/components/
                navbar.jsx 
                footer.jsx
import navbar and footer in app.jsx and create classes (a href)
npm i react_router_dom
import react-router-dom aur 
main.jsx me jao aur <App/> ko wrap kar do--  <BrowserRouter> <App/> </BrowserRouter> 

Make errors Pages 
404 -- notefound.jsx --> Route path="*" element={</Not Found} />
$ Use the Codepen Website to add the animations to the website..!

#Nested Routes 
product/main/newReel -- bs element sambhal ke pass karo after creating the project

#Dynamic Routing --> 
courses/:id
ex. courses/ karne ke baad uski details milni chahiye 

connst par= useParams()--> it just give the parameters of the id 
console.log(par, parId)

# usenavigate Method
    let navigate = useNavigate()
    const btnClicked = ()=>{
    navigate('/')
    }

#Bonus Topics 
--> Data Always flow from top to bottom to get them flow from bottom to top (never happend ) But the trick is 
--> child se parent data tranfer 

# Context API
--> Data Cetralisation , one centralised data unit used by eveyone 

Code-->
ek components aur context ki files aur folder create kar lo 
context api ki data se App ko wrap kar do
then in themecontext.jsx me {props.childern}

steps: create karo context
        provide karo data 
        use karo data ko
